# 課題 1
■必要な要素
1. 本のタイトルを入力する<input>タグ（name属性は"title"、必須入力）
2. 著者名を入力する<input>タグ（name属性は"author"、必須入力）
3. 価格を入力する<input>タグ（name属性は"price"、type属性は"number"）
4. 送信ボタン（type属性は"submit"、表示テキストは"本を登録"）

■追加条件
<form>タグで全体を囲むこと
各入力欄の前には<label>タグで説明を付けること

##  課題1：HTMLで本の登録フォームを作る
やること：
• 	本のタイトル・著者名・価格を入力する欄を作る
• 	「本を登録」って書かれたボタンを作る
• 	全体を <form> タグで囲む
• 	各入力欄の前に説明（<label>）をつける
--- 済11060143

# 課題 2
課題1で作成したフォームに対して、以下の動作をするJavaScriptを書いてください。
■実装する機能
1. フォームの送信イベントを検知する  > ④ JavaScriptで送信を制御する（ステップ1）@
2. ページのリロードを防ぐ > event.preventDefault()@
3. 入力されたタイトル、著者名、価格を取得する > @ **一応priceのデータ型をNumberとして宣言しておく**
4. 取得した値をコンソールに表示する > @

## 課題2：JavaScriptでフォームの動きを作る
やること：
• 	フォームが送信されたときに反応するようにする
• 	ページが勝手にリロードされないようにする
• 	入力された「タイトル・著者名・価格」を取り出す
• 	それをコンソールに表示する（画面じゃなくて開発者ツールのログ）
--- 済11060207

# 課題 3
課題2のコードを拡張して、フォームの入力内容をサーバーに送信するコードを書いてください。
■送信先の情報

URL: http://localhost:3000/api/books
メソッド: POST
ヘッダー: Content-Type: application/json
■送信するデータの形式（JSON）
```json
{
"title": 入力されたタイトル,
"author": 入力された著者名,
"price": 入力された価格
}
```

## 課題3：入力されたデータをサーバーに送る
やること：
• 	JavaScriptで fetch() を使って、サーバーにデータを送る
• 	送る先は http://localhost:3000/api/books
• 	データは JSON形式で送る（例：{ title: "本の名前", author: "著者", price: 1000 }）
---

# 課題 4
課題3のコードに、サーバーからのレスポンスを受信して処理する機能を追加してください。
■実装する機能
1. サーバーからのレスポンスをJSON形式で受け取る
2. 受け取ったデータをコンソールに表示する
3. エラーが発生した場合もコンソールに表示する

##  課題4：サーバーから返ってきた結果を受け取る
やること：
• 	サーバーから返ってきたJSONを受け取る
• 	その内容をコンソールに表示する
• 	エラーが起きたときもコンソールに表示する
--- 

# 課題 5
Node.js と Express を使って、課題4のフロントエンドが送信した本の情報を受け取るサーバー側のコードを書いてください。
■実装する機能
1. ポート番号3000でサーバーを起動する
2. /api/books にPOSTリクエストが来たときの処理を作る
3. 受け取ったtitle、author、priceをコンソールに表示する
4. 以下の形式でJSONレスポンスを返す
```json
{
"status": "success",
"message": "本の登録が完了しました",
"book": { 受け取った本の情報をそのまま含める }
}
```

## 課題5：Node.jsとExpressでサーバーを作る
やること：
• 	ポート3000でサーバーを起動する
• 	/api/books にPOSTされたデータを受け取る
• 	受け取った「タイトル・著者名・価格」をコンソールに表示する
• 	以下のようなJSONを返す：
